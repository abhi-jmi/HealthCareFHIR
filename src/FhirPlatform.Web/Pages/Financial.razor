@page "/financial"
<TypedResourceWorkspace ResourceType="Coverage" Title="Financial administration" Description="Patient coverage, claims, claim responses, explanation of benefits, invoices, accounts, charge items, payment notices, and reconciliations." Tabs="Tabs" Filters="Filters" WorkflowText="Financial workflows provide resource management and visualization only; this platform is not an insurance adjudication engine." />
@code { private static readonly string[] Tabs = ["Coverage", "Claims", "Claim Responses", "EOB", "Invoices", "Accounts", "Payments", "Editor", "Audit"]; private static readonly TypedResourceWorkspace.WorkflowFilter[] Filters = [new("patient", "Patient", "Patient/id"), new("identifier", "Identifier", "claim or coverage id"), new("status", "Status", "active/cancelled/draft")]; }
