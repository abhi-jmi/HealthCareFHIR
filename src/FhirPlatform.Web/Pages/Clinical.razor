@page "/clinical"
<TypedResourceWorkspace ResourceType="Condition" Title="Clinical record keeping" Description="Problem list, allergies, procedure history, family history, care plans, goals, service requests, notes, documents, questionnaires, and responses." Tabs="Tabs" Filters="Filters" WorkflowText="Clinical workflows use coded FHIR resources and CodeableConcepts; diagnoses are not represented as free text only." />
@code { private static readonly string[] Tabs = ["Problems", "Allergies", "Procedures", "Family History", "Care Plans", "Goals", "Documents", "Questionnaires", "Editor", "Provenance"]; private static readonly TypedResourceWorkspace.WorkflowFilter[] Filters = [new("patient", "Patient", "Patient/id"), new("code", "Code", "system|code"), new("clinical-status", "Clinical status", "active/resolved")]; }
