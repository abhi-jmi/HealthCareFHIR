@page "/workflow"
<TypedResourceWorkspace ResourceType="Task" Title="Workflow" Description="Task board, appointments, provider schedules, available slots, service request queue, communications timeline, assignment, and valid status transitions." Tabs="Tabs" Filters="Filters" WorkflowText="Task status transitions are validated in the domain layer; workflow pages use Task, Appointment, Schedule, Slot, ServiceRequest, Communication, and EpisodeOfCare." />
@code { private static readonly string[] Tabs = ["Task Board", "Appointments", "Schedules", "Slots", "Service Requests", "Communications", "Editor", "Audit"]; private static readonly TypedResourceWorkspace.WorkflowFilter[] Filters = [new("patient", "Patient", "Patient/id"), new("status", "Status", "requested/accepted/completed"), new("owner", "Owner", "Practitioner/id")]; }
