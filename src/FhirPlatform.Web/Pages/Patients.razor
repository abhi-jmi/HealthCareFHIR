@page "/patients"
<h2>Patients</h2>
<label class="form-label" for="patient-search">Name or identifier</label><input id="patient-search" class="form-control" placeholder="Search patients" />
<p class="text-muted mt-3">Patient CRUD is performed through the custom API; the UI never calls the FHIR server directly.</p>
