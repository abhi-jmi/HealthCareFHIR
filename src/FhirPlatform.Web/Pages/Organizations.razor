@page "/organizations"
<TypedResourceWorkspace ResourceType="Organization" Title="Organization hierarchy" Description="Organization hierarchy, departments, service ownership, endpoints, and contact management persisted as FHIR references." Tabs="Tabs" Filters="Filters" WorkflowText="Organization hierarchy is represented with Organization.partOf and related Location, HealthcareService, and Endpoint references rather than UI-only state." />
@code { private static readonly string[] Tabs = ["Hierarchy", "Facilities", "Departments", "Services", "Endpoints", "Contacts", "Editor", "Audit"]; private static readonly TypedResourceWorkspace.WorkflowFilter[] Filters = [new("name", "Name", "organization"), new("identifier", "Identifier", "system|value"), new("active", "Active", "true/false")]; }
